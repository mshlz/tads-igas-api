{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "launch api",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}",
      "resolveSourceMapLocations": ["!**/node_modules/**", "!node:internal/**"]
    }
  ]
}
